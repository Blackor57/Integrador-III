# Nombre de la aplicación y Puerto
spring.application.name=Restaurants
server.port=8080

# Configuración de la Base de Datos (Railway)
# ¡Nota que los parámetros ahora están correctamente unidos con el signo "?" al final de la URL!
spring.datasource.url=jdbc:mysql://hayabusa.proxy.rlwy.net:47843/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=psWuejQDEWqogRnmWGvuIkQjdrhkUfRK
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuración para servir archivos HTML puros (Sin Thymeleaf)
# Esto le dice a tu controlador que busque archivos dentro de la ruta estática y le agregue ".html" al final de lo que retornes.
spring.mvc.view.prefix=/
spring.mvc.view.suffix=.html